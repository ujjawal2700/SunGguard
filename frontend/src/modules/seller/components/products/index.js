/**
 * Barrel for product-management sub-components.
 *
 * Scaffolded in refactor P4.4 (Part 3). Currently empty — components will be
 * added one at a time as `seller/pages/ProductManagement.jsx` is decomposed.
 */
export {};
