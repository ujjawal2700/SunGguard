# `modules/seller/components/returns/`

Per-page subcomponent home for `pages/Returns.jsx` (48 KB).
Scaffolded as part of refactor P4.7 (Part 3).

## Target layout

```
modules/seller/components/returns/
├── ReturnsTable.jsx           # paginated list with status badges
├── ReturnFilters.jsx          # status + date range + search
├── ReturnRequestModal.jsx     # approve/reject form with reason
├── ReturnDetailsDrawer.jsx    # right-side drawer with item rows + images
└── index.js
```

See `frontend-page-decomposition` skill for the extraction rules.

## Migration status

| Component               | Status   |
| ----------------------- | -------- |
| Scaffolding             | complete |
| ReturnsTable.jsx        | pending  |
| ReturnFilters.jsx       | pending  |
| ReturnRequestModal.jsx  | pending  |
| ReturnDetailsDrawer.jsx | pending  |
